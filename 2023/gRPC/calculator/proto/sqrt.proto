syntax = "proto3";
package calculator;
option go_package = "github.com/AdamDomagalsky/goes/2023/gRPC/calculator/proto";

message SqrtRequest {
    double number = 1; // could be uint but we want to make negative sqrt to fail
}

message SqrtResponse {
    double result = 1;
}